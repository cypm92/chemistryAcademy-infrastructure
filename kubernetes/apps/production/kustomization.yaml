apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The production cluster references this composition after the SOPS secrets and
# immutable GHCR image tags have been verified.
resources:
  - namespace.yaml
  - settings-configmap.yaml
  - postgres.yaml
  - backend.yaml
  - frontend.yaml
